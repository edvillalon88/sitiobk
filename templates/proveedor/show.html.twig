{% extends 'base.html.twig' %}


{% block body %}
    <h1>Proveedor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proveedor.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proveedor_index') }}">Volver a listado</a>

    <a href="{{ path('proveedor_edit', {'id': proveedor.id}) }}">Editar</a>

    {{ include('proveedor/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}


{% block body %}
    <h1>Galería</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galeria.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('galeria_index') }}">Volver a listado</a>

    <a href="{{ path('galeria_edit', {'id': galeria.id}) }}">Editar</a>

    {{ include('galeria/_delete_form.html.twig') }}
{% endblock %}
